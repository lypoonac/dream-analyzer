streamlit==1.28.0
transformers==4.35.0  # This works with Torch >=1.10, so 2.10.0 is fine
torch==2.10.0         # Change to this compatible version
pandas==2.1.3
protobuf==3.20.3
sentencepiece==0.1.99
rich>=10.14.0         # Add this if you want improved logging (as in the log)
